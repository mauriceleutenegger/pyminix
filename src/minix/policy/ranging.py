"""Monitor range checking (§6.4).

Tolerance bands with the additive 1.0 term, and the HV-off test delay. The
result drives an out-of-range indicator only; like the reference, this
project does not drop HV on a range failure.
"""
